Add live WebUI health probes to diagnostics and folder-picker peer/state labels

- resilio_client: add async_get_app_info, async_get_version,
  async_get_performance_warnings, async_get_statuses WebUI actions.
- diagnostics: run these live at report time (independent of the
  coordinator's cached data), capturing per-probe failures instead of
  raising.
- config_flow: folder picker options now show a sync-state symbol plus
  connected/total peer counts (e.g. "check Backups (2/3 peers
  connected)").

Co-authored-by: Copilot App <223556219+Copilot@users.noreply.github.com>
